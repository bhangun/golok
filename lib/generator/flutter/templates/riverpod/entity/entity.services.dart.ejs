import 'dart:async';
import 'dart:convert';

import '../model/<%= snakeCase %>.dart';
import '../../../services/rest/rest_services.dart';


class <%= titleCase %>Services {

  static Future<<%= titleCase %>> <%= camelCase %>(String id) async {
    var response = await RestServices.fetch('/api/<%= camelCase %>$id');
    return <%= titleCase %>.fromJson(json.decode(response));
  }

  static Future<List<<%= titleCase %>>> <%= camelCase %>s([var page, var size, var sort]) async {
    var data = await RestServices.fetch('/api/<%= camelCase %>s');
    return <%= titleCase %>.listFromString(data);
  }

  static create<%= titleCase %>(<%= titleCase %> <%= camelCase %>) async {
    
  }

  //
  static update<%= titleCase %>(<%= titleCase %> <%= camelCase %>) async {
    
  }

  //
  static delete<%= titleCase %>(int id) async {
    
  }

}