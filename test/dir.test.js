import { typeCheck } from '../lib/utils.cjs';

describe('sum module', () => {
    test('current directory', () => {
        console.log(typeCheck('{}'));
        //expect(sum(1, 2)).toBe(3);
    });
});