import {
  Framework,
  Platform,
  TechnologyLayer,
  type TemplateProfile,
} from "../core/models.ts";

export const templateRegistries: TemplateProfile[] = [{
  name: "Flutter",
  manifestPath: "manifest.yaml",
}];
